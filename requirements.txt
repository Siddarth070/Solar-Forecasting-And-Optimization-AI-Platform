# ============================================================
# Solar Forecast Platform — Python Dependencies
# ============================================================
# Every dependency is pinned exactly (==). This is the set actually
# installed, tested, and run against in CI (.github/workflows/tests.yml)
# — pip install -r requirements.txt in a clean venv is verified to work.
# Bump a version deliberately, then re-run the full test suite + `make
# train` + `make benchmark` before committing the bump.

# ── Core data ───────────────────────────────────────────────
pandas==3.0.6
numpy==2.4.6
pyarrow==25.0.1          # Parquet read/write

# ── HTTP & config ────────────────────────────────────────────
requests==2.33.1
pyyaml==6.0.1
python-dotenv==1.2.3

# ── Logging ─────────────────────────────────────────────────
loguru==0.7.3

# ── Validation ──────────────────────────────────────────────
pydantic==2.13.5

# ── ML models ─────────────────────────────────────────────────
scikit-learn==1.9.1
xgboost==3.2.0
# tensorflow==2.13.0     # Needed only to retrain/run the LSTM model (notebooks/) — not in the served product
# prophet==1.1.4         # Needed only to retrain/run the Prophet model (notebooks/) — not in the served product

# ── Feature engineering ──────────────────────────────────────
pvlib==0.15.2            # Solar-position clear-sky model (src/features/pipeline.py)

# ── Optimization ──────────────────────────────────────────────
pulp==3.3.2              # Used by src/optimization/battery_optimizer.py

# ── API layer ─────────────────────────────────────────────────
fastapi==0.141.1
uvicorn==0.53.0

# ── Dashboard ─────────────────────────────────────────────────
streamlit==1.64.0
plotly==7.1.0

# ── Testing ─────────────────────────────────────────────────
pytest==9.1.1
pytest-cov==7.1.0
httpx==0.28.1            # Required by fastapi.testclient.TestClient

# ── Development utilities ────────────────────────────────────
jupyter==1.1.1
ipykernel==7.3.0
